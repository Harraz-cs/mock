apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sapcapchart.fullname" . }}-test-connection"
  labels:
    {{- include "sapcapchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sapcapchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
